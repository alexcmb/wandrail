apiVersion: 1

datasources:
  # Supabase (production) — donnees reelles
  - name: Wandrail Supabase
    type: postgres
    uid: wandrail_pg
    url: aws-0-eu-west-1.pooler.supabase.com:6543
    user: postgres.tqzvujolklzcaytvaajo
    secureJsonData:
      password: "${SUPABASE_PASSWORD}"
    jsonData:
      database: postgres
      sslmode: require
      maxOpenConns: 5
      maxIdleConns: 2
      postgresVersion: 1500
      timescaledb: false
    isDefault: true
    editable: true
